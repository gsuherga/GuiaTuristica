<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.guiaturistica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PantallaPrincipal.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para la pantalla principal de monumentos_menu_fotos !-->
        <activity android:name=".Monumentos.MonumentosActivity"
            android:finishOnTaskLaunch="true">
        </activity>

        <!-- Actividad para la pantalla de la Catedral!-->
        <activity android:name=".Monumentos.Catedral.Catedral"
            android:finishOnTaskLaunch="true">
        </activity>

        <!-- Actividad para la pantalla del Alcázar!-->
        <activity android:name=".Monumentos.Alcazar.Alcazar"
            android:finishOnTaskLaunch="true">
        </activity>

        <!-- Actividad para la pantalla del Archivo!-->
        <activity android:name=".Monumentos.Archivo.Archivo"
            android:finishOnTaskLaunch="true">
        </activity>

        <!-- Actividad para la pantalla de Saborea Sevilla!-->
        <activity android:name=".Saborea.SaboreaSevilla"
            android:finishOnTaskLaunch="true">
        </activity>

    </application>

</manifest>